.section text
_start:
    mov x5, 47  ; nth

    mov x0, 0   ; return value
    mov x1, 1
    mov x3, x5

.loop:
    add x0, x1
    mov x2, x0

    mov x0, x1
    mov x1, x2

    sub x3, 1
    cmp x3, 0
    jne .loop

    hlt
